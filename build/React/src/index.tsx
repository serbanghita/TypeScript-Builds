import * as React from "react";
import * as ReactDOM from "react-dom";
import {App} from "./components/App";

ReactDOM.render(<App title="React and TypeScript demo!"/>, document.getElementById("app"));